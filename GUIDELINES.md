# AI PR Reviewer - 开发规范与评分指南 (GUIDELINES)

本篇文档汇总了本项目（AI 代码评审助手）的所有原始需求、评分规则、迭代规范以及 PR 和 commit 要求，作为持续开发与测试中的核心准则，规避幻觉与规则遗漏。

---

## 🎯 一、 核心功能与技术需求

### 1. 核心定位
开发一个以 AI 辅助分析为核心的代码评审工具，帮助开发者提升 Pull Request (PR) 的 Review 效率与质量。

### 2. 功能清单
- [x] **PR 变更总结 (PR Summary)**: 智能获取代码变更并输出功能概要、关键修改点和架构影响。
- [x] **风险代码识别 (Risk Identification)**: 智能评估 PR 的风险指数，输出百分制风险评分与风险总结，捕捉潜在的安全泄漏、致命逻辑漏洞或性能瓶颈。
- [x] **Review 建议生成 (Review Suggestions)**: 还原经典 Diff 视图并在被修改代码行下就地渲染 AI 审查建议。意见包含严重程度分级（Critical/Warning/Info）、问题描述及可一键复制的应用修复建议。
- [x] **GitHub 同步 (GitHub Integration)**: 一键向 GitHub 对应 PR 写入正式 Review 评论及行级评审卡片。

### 3. 关键性能与体验设计要求
- **分析准确性与上下文理解**: 精准抽取 diff 信息，提供关联具体文件和正确行号的逻辑审查。
- **误报与漏报控制**: 
  - 通过 Prompt 调优限制 AI 对无关排版格式发表无建设性意见。
  - **行号对齐过滤机制**：比对 diff 补丁中合规的右侧行号，剔除幻觉评论，对错位建议自动分流并作为总报告兜底输出，避免 GitHub API 422 报错。
- **响应速度与体验**:
  - 本地 Express 服务代理，实现**零密钥前端体验**（Keyless client config），提升密钥安全防漏等级。
  - **本地历史仪表盘**：对最近 10 条评审记录进行本地快照持久化，实现秒级二次加载，规避重复 API 请求。
  - 首屏引导极简设计，折叠 Token 配置；在 suggestions 工作区提供对齐 GitHub 按需/就地配置 Token 同步。
- **未来扩展思路说明**: 在 README.md 中明确论证模型选择（Qwen-Plus/Max/Coder）、上下文解析方式以及后期 CI/CD 流水线集成等演进规划。

---

## 🏆 二、 大赛评分规则 (Grading Weights)

* **40% - 作品完整度与创新性**: 产品设计合理性、功能完整度、交互流畅度、是否新颖有创意。
* **40% - 开发过程与质量**: 架构清晰度与合理性、代码健壮度（逻辑、规范、可读性等）、PR 数量与质量、commit 分布合理性。
* **20% - 演示与表达**: demo 视频清晰完整度、是否清晰完整表达作品功能和效果。

---

## 🔄 三、 交付迭代与分支管理规范

### 1. 全周期持续交付要求
- 严禁临尾“突击提交”。
- 从开发首日起，应在整个开发周期内保持**持续的 PR (Pull Request) 记录和 commit 提交**，体现完整开发链路演进。

### 2. PR 粒度与单一职责原则
- **每个 PR 只做一件事**：每个 PR 仅实现或修改单一功能。
- 鼓励尽可能小、粒度尽可能细的 PR。大功能必须拆分为多个独立 PR 分步提交。
- PR 合并后，主分支代码需保持**完全可运行状态**，可随时进行效果复现与演示。

### 3. PR 标题与描述规范
每个 PR 的标题和描述必须清晰完整，包含以下四要素：
1. **① 标题**：一句话说明本 PR 新增/修改了什么（例如 `feat: add ...`）。
2. **② 功能描述**：说明该功能的作用与具体使用方式。
3. **③ 实现思路**：简要说明技术选型、架构思路或核心实现逻辑。
4. **④ 测试方式**：说明如何验证该功能正常运行（附带测试命令或操作指引）。

---

## 💻 四、 Commit 提交规范

保持有意义的 commit 记录，严禁出现诸如 `temp`、`fix`、`111` 等无意义的 message。推荐遵循 Angular 规范：
* `feat`: 新功能 (feature)
* `fix`: 修补 bug
* `docs`: 文档变更 (documentation)
* `style`: 格式、样式（不影响代码运行的变动）
* `refactor`: 重构（即不是新增功能，也不是修改 bug 的代码变动）
* `test`: 增加测试或修改测试
* `chore`: 构建过程或辅助工具的变动
